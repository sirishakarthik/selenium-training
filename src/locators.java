import java.io.File;

import org.openqa.selenium.By;
import org.openqa.selenium.Keys;
import org.openqa.selenium.OutputType;
import org.openqa.selenium.TakesScreenshot;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.WindowType;
import org.openqa.selenium.chrome.ChromeDriver;
import org.testng.Assert;

public class locators {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		/*
		 * System.setProperty("webdriver.chrome.driver",
		 * "C:/Users/forou/Downloads/chromedriver_99/ChromeDriver.exe");
		 * 
		 * WebDriver driver = new ChromeDriver(); driver.get("https://qa.carnow.com");
		 * //driver.findElement(By.xpath("//a[@href='#features']")).click();
		 * 
		 * //driver.findElement(By.cssSelector(("a[href='/users/sign_in']"))).click();
		 * //driver.findElement(By.name("user[login]")).sendKeys("user_2");
		 * //driver.findElement(By.id("user_password")).sendKeys("Please11");
		 * //driver.findElement(By.cssSelector(("input[value='Sign In']"))).click();
		 * //driver.findElement(By.cssSelector("span.ng-binding"));
		 * //driver.findElement(By.cssSelector("p[data-text='Log Out']")).click();
		 * //driver.switchTo().alert().dismiss(); File src=
		 * ((TakesScreenshot)driver).getScreenshotAs(OutputType.FILE);
		 * Fileutils.copyfile(src, new )
		 * 
		 * }
		 * 
		 * private static Object TakesScreenshot(WebDriver driver) { // TODO
		 * Auto-generated method stub return null; }
		 */
	}
}

// driver.findElement(By.id("user_login")).sendKeys("test@user");
// driver.findElement(By.name("user[password]")).sendKeys("Please11");

// driver.findElement(By.cssSelector("input.button-default")).click();
// driver.switchTo().newWindow(WindowType.TAB);
// driver.navigate().to("https://qa.carnow.com/test?f=nightlysignout");
// driver.navigate().back();
// System.out.println(driver.findElement(By.cssSelector("span.text-bold")).getText());
// Assert.assertEquals(driver.findElement(By.cssSelector("span.text-bold")).getText(),
// "Invalid login or password.");

// driver.findElement(By.cssSelector("body")).sendKeys(Keys.CONTROL + "t");

// driver.get("https://qa.carnow.com/test?f=nightlysignout");

// driver.findElement(By.xpath("//a[@href='/users/password/new']")).click();
//https://qa.carnow.com/conversations
// driver.findElement(By.xpath("//nav/div/div/div[2]/a[1]/following-sibling::a[3]")).click();
